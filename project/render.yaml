# Exported from Render on 2025-12-22T06:50:34Z
version: "1"
projects:
- name: My project
  environments:
  - name: Production
    services:
    - type: web
      name: Aquila
      runtime: node
      repo: https://github.com/rakomsakom/project
      plan: free
      envVars:
      - key: Value
        sync: false
      - key: DATABASE_URL
        sync: false
      region: frankfurt
      buildCommand: npm install
      startCommand: npm start
      autoDeployTrigger: commit
